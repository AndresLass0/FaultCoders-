# Hacer commits en github

```
Formula de Sumatoria de Gauus:

(Sumatoria de todos los numero de 1 hasta N)

(n*(n+1))/2;


Formula de sumatoria de Gauus(Inversa):
(Dada la sumartoria N, me devuelve hasta que numero hizo la sumatoria)
Ejemplo: N = 6, entonces 1, 2, 3 <- Resultado = 3

(sqrt((n*8)+1)-1)/2;



Simplificar una fraccion:
(Imaginemos 3/6, El resultado sera: 1/2)

void simplify() {
    int mcd = __gcd(abs(a), abs(b));
    a /= mcd;
    b /= mcd;
  }




```
